{
  "name": "@squirl/nova",
  "bin": "./build/index.js",
  "files": ["build/*"],
  "scripts": {
    "prepack": "bun build --outdir=build --target=bun --minify index.ts"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}

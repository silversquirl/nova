{
  "name": "@squirl/nova",
  "version": "0.1.0",
  "bin": { "nova": "build/index.js" },
  "files": ["build/*"],
  "scripts": {
    "prepack": "bun build --outdir=build --target=bun --minify index.ts"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
